name: Aktualizovat data a publikovat web

on:
  push:
    branches: [main]
  schedule:
    - cron: "17 7-22 * * *"
      timezone: "Europe/Prague"
  workflow_dispatch:
    inputs:
      test_telegram:
        description: Poslat testovací Telegram zprávu
        required: false
        type: boolean
        default: false
      send_report:
        description: Poslat jednorázový přehled aktuálně dostupných vozů
        required: false
        type: boolean
        default: false
      report_models:
        description: Modely oddělené čárkou
        required: false
        type: string
        default: Scala,Kamiq
      report_min_price:
        description: Cena od v Kč
        required: false
        type: string
        default: "370000"
      report_max_price:
        description: Cena do v Kč
        required: false
        type: string
        default: "420000"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: skoda-hlidac
  cancel-in-progress: false

jobs:
  update-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Stáhnout repozitář
        uses: actions/checkout@v6

      - name: Nastavit Python
        if: github.event_name != 'push'
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Stáhnout a porovnat nabídky
        if: github.event_name != 'push'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TEST: ${{ inputs.test_telegram && '1' || '0' }}
          TELEGRAM_REPORT: ${{ inputs.send_report && '1' || '0' }}
          TELEGRAM_REPORT_MODELS: ${{ inputs.report_models }}
          TELEGRAM_REPORT_MIN_PRICE: ${{ inputs.report_min_price }}
          TELEGRAM_REPORT_MAX_PRICE: ${{ inputs.report_max_price }}
        run: python scraper/update.py

      - name: Uložit aktuální data
        if: github.event_name != 'push'
        run: |
          if git diff --quiet -- data/latest.json; then
            echo "Data se nezměnila."
            exit 0
          fi
          git config user.name "skoda-hlidac-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/latest.json
          git commit -m "data: aktualizace nabídek"
          git push

      - name: Nastavit GitHub Pages
        uses: actions/configure-pages@v5

      - name: Připravit web
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

      - name: Publikovat GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
